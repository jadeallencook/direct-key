import React from 'react';
import './Navigation.scss';

function Navigation() {
    return (
        <nav>
            <span>Onity</span>
            <button>Request Info</button>
        </nav>
      );
}

export default Navigation;