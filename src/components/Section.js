import React from 'react';
import './Section.scss';

function Section() {
    return (
        <section className="Section">
            Section is working...
        </section>
      );
}

export default Section;