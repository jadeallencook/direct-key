import React from 'react';
import './Header.scss';

function Header() {
    return (
        <header className="Header">
            Header is working...
        </header>
      );
}

export default Header;